name: splasher
description: "The Splasher widget in Flutter is designed to create a splash screen for your application."
version: 0.0.2
homepage: 'https://magradze.dev'
repository: 'https://github.com/magradze/splasher'

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  lottie: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: